apiVersion: v1
kind: Service
metadata:
  name: react-frontend
  namespace: ns-akshit-pipeline-app-1
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: react-frontend
