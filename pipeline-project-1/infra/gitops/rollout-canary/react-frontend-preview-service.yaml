apiVersion: v1
kind: Service
metadata:
  name: react-frontend-preview
  namespace: akshit-pipeline-app-1
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: react-frontend
