apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: ns-akshit-pipeline-app-1
spec:
  selector:
    app: prometheus
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 9090
      name: http
  type: LoadBalancer
