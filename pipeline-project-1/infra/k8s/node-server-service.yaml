apiVersion: v1
kind: Service
metadata:
  name: node-server-service
  namespace: ns-akshit-pipeline-app-1
spec:
  selector:
    app: node-server
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http
  type: ClusterIP
